query FindTripQuery($from: String!, $to: String!) {
  trip(
    from: {
      place: $from
    }
    to: {
      place: $to
    }
    numTripPatterns: 3
  ) {
    tripPatterns {
      aimedStartTime
      expectedStartTime
      duration
      legs {
        mode
        duration
        distance
        toPlace {
          name
        }
        line {
          name
          id
        }
      }
    }
  }
}

query stopsQuery {
  stopPlaces {
    id
    name
  }
}